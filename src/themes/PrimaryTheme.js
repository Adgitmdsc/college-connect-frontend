import { createTheme } from '@mui/material/styles'

export const PrimaryTheme = createTheme({
    palette: {
        primary: {
            main: "#fff",
            light: "#fff",
            dark: "#fff"
        },
        secondary: {
            main: "#CCD7C5",
            light: "#EFD2CB",
            dark: "#826754"
        }
    }
})