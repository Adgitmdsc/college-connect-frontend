import { createTheme } from '@mui/material/styles'

export const PrimaryTheme = createTheme({
    palette: {
        primary: {
            main: "#fff",
            light: "#fff",
            dark: "#fff"
        }
    }
})